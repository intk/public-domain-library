export default () => {
  // // calculate the height of result search
  // const search = document.querySelector('[data-search]')
  // if (search && window.innerWidth > 1024) {
  //   setTimeout(() => {
  //     const searchContainer = document.querySelector('[data-search-container]')
  //     if (searchContainer.offsetHeight > 650) {
  //       const searchContainerHeight = searchContainer.offsetHeight + 411
  //       search.style.height = `${searchContainerHeight}px`
  //     }

  //     window.addEventListener('resize', () => {
  //       const searchContainer = document.querySelector('[data-search-container]')
  //       if (searchContainer.offsetHeight > 650) {
  //         const searchContainerHeight = searchContainer.offsetHeight + 411
  //         search.style.height = `${searchContainerHeight}px`
  //       }
  //     })
  //   }, 200)
  // }
}
